pipeline {
    agent { dockerfile true }
    stages {
        stage('Version') {
            steps {
                sh 'node --version'
            }
        }
         stage('Mount') {
            steps {
                sh 'mount -o remount,exec /var'
            }
        }
    }
}