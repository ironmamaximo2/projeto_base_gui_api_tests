pipeline {
    agent { dockerfile true }
    stages {
        stage('Version') {
            steps {
                sh 'node --version'
            }
        }
         stage('Version') {
            steps {
                sh 'mount -o remount,exec /var'
            }
        }
    }
}