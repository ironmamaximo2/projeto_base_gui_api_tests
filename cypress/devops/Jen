pipeline {
    agent { dockerfile true }
    stages {
        stage('Version') {
            steps {
                sh 'node --version'
            }
        }
         stage('Mount') {
            steps {
                sh 'sudo mount -o remount,exec /var'
            }
        }
    }
}