pipeline {
    agent {
        docker { image 'node:14-alpine' }
    }
    environment {
        HOME = '.'
    }

        stage('Test') {
      steps {
        sh 'node --version'
      }
        }
}
}
