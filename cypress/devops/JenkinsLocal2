pipeline {
    agent {
        docker { image 'node:14-alpine' }
    }
    stages {
    stage('Test2') {
      steps {
        sh 'sudo npm install --unsafe-perm node-sass'
      }
    }
        stage('Test') {
      steps {
        sh 'node --version'
      }
        }

    }
}
