pipeline {
    agent {
        docker { image 'node:14-alpine' }
        
    }
    stages {
        stage('node version') {
            steps {
                sh 'node --version'
            }
        }
        
        stage('run tests') {
            steps {
                sh 'npm run test_all'
            }
        }
    }
}