pipeline {
    agent {
        docker { image 'timbru31/node-alpine-git' }
        
    }
    stages {
        stage('node version') {
            steps {
                sh 'node --version'
            }
        }
        
        stage('run tests') {
            steps {
                sh 'npm run test_all'
            }
        }
    }
}