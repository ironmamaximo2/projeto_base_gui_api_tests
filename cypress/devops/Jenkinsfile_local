
pipeline {
    agent { dockerfile true{
      additionalBuildArgs "--build-arg user_id=${user_id} --build-arg group_id=${group_id}"
    } }
    stages {
        
        stage('Test E2E') {
            steps {
                sh 'node --version'
                sh 'npm run test_all'
            }
        }
    }
}