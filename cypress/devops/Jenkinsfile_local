pipeline {
        agent {
            docker {
            args '-u root:sudo'
         }
        }
        
    stages {
        stage('Version') {
            steps {
                sh 'node --version'
            }
        }
        stage('Test') {
            steps {
                sh 'npm run test_all'
            }
        }
    }
}