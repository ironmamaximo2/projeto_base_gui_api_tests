pipeline {
        agent {
        dockerfile true {
            image 'ubuntu'
            args '-u root:sudo'
        }
    }
        
    stages {
        stage('Version') {
            steps {
                sh 'node --version'
            }
        }
        stage('Test') {
            steps {
                sh 'npm run test_all'
            }
        }
    }
}