
pipeline {
    agent { dockerfile true }
    stages {
        
        stage('Test E2E') {
            steps {
                sh 'node --version'
                sh 'npm run test_all_pipe'
            }
        }
    }
}